# Secrets and local state — never commit these.
config.json
.env
leads.db
leads.db-journal
leads.db-wal
leads.db-shm

# Generated client sites (Phase 3+).
sites/

# Sample sites, rebuilt from demo/fixtures/ by `leadsmith demo` in a few
# seconds. The fixtures are the source; these are the output, photographs and
# all, and there is no reason to carry a few megabytes of stock JPEG in git.
demos/

# The operator's dashboard (Phase 6). Every client's revenue is on it.
board.html

# Invoices (Phase 7). Clients' names, addresses and what they were charged.
invoices/
invoices.csv

__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/
